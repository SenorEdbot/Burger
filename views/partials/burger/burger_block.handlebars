<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}TIME TO EAT!{{else}}I'M FULL!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>